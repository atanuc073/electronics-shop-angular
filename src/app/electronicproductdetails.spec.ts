import { Electronicproductdetails } from './electronicproductdetails';

describe('Electronicproductdetails', () => {
  it('should create an instance', () => {
    expect(new Electronicproductdetails()).toBeTruthy();
  });
});
